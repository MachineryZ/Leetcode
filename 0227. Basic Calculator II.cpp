class Solution {
public:
    int calculate(string s) {
        int n = s.size();
        int num = 0;
        stack<int> st_num;
        stack<char> st_op;
        for (int i = 0; i < n; i++) {
            if (s[i])
        }
    }
};